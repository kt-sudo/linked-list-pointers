# Linked List Pointers Demonstration
Understand how pointers, double pointers are associated with linked list nodes

This project demonstrates:
- ptr = currentNode->next
- ptr = &currentNode->next
- ptr = *currentNode->next

Language: C
